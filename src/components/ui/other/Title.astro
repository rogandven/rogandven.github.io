---
import { slugify } from '../../../utils/general.utils';

interface Props {
    text?: string,
    _class?: string
}

const { 
  text = "",
  _class = "",
} = Astro.props;
---
<h2 id={slugify(text)} class=`text-4xl font-extrabold w-full ${_class} text-center`>{text}</h2>
